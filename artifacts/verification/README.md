# Verification artifacts

Each headless run creates one directory named by its correlation ID. The raw
manifest, VM trace, and supervisor phase sequence are evidence; this directory
does not contain canonical Universe state.
