# VM benchmark evidence

No scale claim is made for Graph IR v0 yet. Future runs must record the exact
fixture hash, hardware, iteration count, allocations, memory, throughput, and
p50/p95/p99 latency.
